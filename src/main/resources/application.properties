# DATABASE CONFIGURATION
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.database-platform=${DB_DIALECT}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.show-sql=false
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=create
#spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=NEVER
spring.output.ansi.enabled=ALWAYS

# URLs CORS CONFIGURATION
cors.allowed-origins=http://localhost:4200,http://localhost:8080

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Email Notifications
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aquavision.notifications@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# MQTT Service
# CUIDADO CON ESTO:
# el client id deployado es "spring-backend-listener", si se pone ese mismo nombre y
# se levanta el proyecto local, se superponen los clientes del broker MQTT y solo va a
# tomar uno los paquetes que manden los sensores reales instalados. Como el deployado
# ya está con ese nombre, siempre que se quiera levantar local hay que cambiar nombre
app.mqtt.client-id=spring-backend-listener2
#app.mqtt.broker=ssl://m171d340.ala.us-east-1.emqxsl.com:8883
app.mqtt.broker=ssl://p1b6c61d.ala.us-east-1.emqxsl.com:8883
#app.mqtt.username=test
app.mqtt.username=aquabroker
app.mqtt.password=test123
app.mqtt.topic=mediciones
